services:
  docs-validator:
    build:
      context: ..
      dockerfile: docs-validate/Dockerfile
    image: frp-tools-docs-validate:latest
    working_dir: /workspace
    volumes:
      - ..:/workspace
    command: mkdocs build --strict
